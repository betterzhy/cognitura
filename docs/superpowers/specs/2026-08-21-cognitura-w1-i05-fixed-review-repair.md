# W1-I05 Fixed-Candidate Review Repair

```text
RepairKind = APPEND_ONLY_FIXED_CANDIDATE_REVIEW_REPAIR
ReviewVerdict = NO_GO_P0_0_P1_3_P2_1
RepairOriginSHA = f2f8faa005e2852000cd59cac0c5d9ec98fb6cfd
FindingRedSHA = 558ec44a0d35e0479940ebfb5fbd4c8f8c2f29b5
FindingGreenSHA = 46c519f6dc79e9d12f4485a2ed9469c350b76a46
FormalDatabaseWrite = NOT_AUTHORIZED
RemotePush = NOT_AUTHORIZED
```

The fixed-candidate review found three product P1 findings and one governance-test P2
finding. The fixed RED/GREEN pair closes the I05-owned product findings without
implementing I06 relationship or media behavior:

- table and preceding paragraph image events advance the container's global order;
- table image offsets are Unicode code-point offsets with placeholder bijection;
- literal `U+FFFC` source text fails closed instead of inventing an image;
- table grid size is bounded before dense allocation and paragraph joining is linear;
- candidate and merge geometry use overflow-safe arithmetic.

The remaining P2 repair must make the XML copy-admission wrong-evidence fixture place
the wrong blob in the first declared specification commit and assert the production
`evidence mismatch: step 0` diagnostic. The historical XML-copy matrix must execute
the fixed verifier blob from `RepairOriginSHA`; it must not copy the evolving worktree
verifier and thereby become self-referential.

After `FindingGreenSHA`, the exact append-only review repair sequence is:

```text
1. docs/superpowers/specs/2026-08-21-cognitura-w1-i05-fixed-review-repair.md
2. tests/task-cards/verify-wave1-implementation-cards.sh
3. scripts/verify-wave1-implementation-cards
```

The final verifier binds the fixed RED/GREEN commit identities, the specification and
test evidence blobs, then restores the exact I05-only descendant rule. No I05 closure,
I06 release, generic maintenance lane, database authorization, history rewrite, or
remote push is permitted.
